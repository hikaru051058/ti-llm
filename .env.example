# Common env settings
AWS_PROFILE=your-aws-profile
AWS_REGION=us-west-2

# Factory secret used by the ESP (HMAC for provisioning)
FACTORY_SECRET=replace-with-hex-secret

# Gateway API URLs (set after deploy or if known)
PROVISION_URL=https://REMOVED/prod/provision
ASK_URL=https://REMOVED/prod/ask
